<Window x:Class="MediaPlayerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayerApp"
        xmlns:UserControl="clr-namespace:MediaPlayerApp.Views"
        mc:Ignorable="d"
        Icon="/Icons/logo.png"
        Title="Duducha" Height="750" Width="1000">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <MediaElement x:Name="mediaElement" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" LoadedBehavior="Manual"/>
        <UserControl:SongThumbnailAndDetails />

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,0,0,10">
            <UserControl:MediaControls  />
        </StackPanel>


        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Open" Width="80" Margin="5" Click="OpenButton_Click"/>
            <Button Content="Play" Width="80" Margin="5" Click="PlayButton_Click"/>
            <Button Content="Pause" Width="80" Margin="5" Click="PauseButton_Click"/>
            <Button Content="Stop" Width="80" Margin="5" Click="StopButton_Click"/>
            <Slider x:Name="volumeSlider" Width="100" Minimum="0" Maximum="1" Value="0.5" Margin="5" ValueChanged="VolumeSlider_ValueChanged"/>
        </StackPanel>

    </Grid>
</Window>
