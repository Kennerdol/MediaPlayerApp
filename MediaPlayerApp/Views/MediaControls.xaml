<UserControl x:Class="MediaPlayerApp.Views.MediaControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPlayerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="600">
    
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- ⏳ Progress Bar -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <TextBlock Text="0:45" Foreground="Black" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ProgressBar Minimum="0" Maximum="100" Value="30" Name="pbStatus" Foreground="#0370c8" Width="500" Height="5" Cursor="Hand" />
            <TextBlock Text="3:45" Foreground="black" FontSize="12" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Name="Open" >
                <Image Source="/Icons/add.png" />
            </Button>
            <Button Name="Play">
                <Image Source="/Icons/play.png" />
            </Button>
            <Button Name="Previous">
                <Image Source="/Icons/previous.png" />
            </Button>
            <Button Name="Stop">
                <Image Source="/Icons/stop.png" />
            </Button>
            <Button Name="Next">
                <Image Source="/Icons/next.png" />
            </Button>
            <Button Name="Repeat">
                <Image Source="/Icons/repeat.png" />
            </Button>
            <Button Name="Shuffle">
                <Image Source="/Icons/shuffle.png" />
            </Button>
            <Button Name="Playlis" >
                <Image Source="/Icons/playlist.png" />
            </Button>

            <Grid>
                <!-- Speaker Button -->
                <Button x:Name="speakerButton" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="/Icons/speaker.png" Stretch="Uniform"/>
                </Button>

                <!-- Volume Popup -->
                <Popup x:Name="volumePopup"
                   Placement="Right"
                   StaysOpen="False"
                   AllowsTransparency="True">
                    <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="5">
                        <Slider x:Name="volumeSlider" Width="100" Minimum="0" Maximum="100" Value="15" Orientation="Horizontal"/>
                    </Border>
                </Popup>

            </Grid>

        </StackPanel>
    </Grid>
</UserControl>
